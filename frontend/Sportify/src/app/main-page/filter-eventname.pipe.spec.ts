import { FilterEventnamePipe } from './filter-eventname.pipe';

describe('FilterEventnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEventnamePipe();
    expect(pipe).toBeTruthy();
  });
});
